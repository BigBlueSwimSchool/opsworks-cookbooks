<% node['services'].each do |serviceName, service| -%>

upstream <%= serviceName %> {
	server <%= service.socket %>;
}

<% end %>

<% node['deploy'].each do |application, deploy| -%>

# upstream <%= application %> {
#    server /var/run/<%= application %>.sock
#}

<% end %>